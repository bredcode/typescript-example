<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>🏆 업다운 랭킹</title>
    <style>
      body {
        font-family: "Segoe UI", sans-serif;
        background: #f4f4f4;
        padding: 30px;
      }
      h1 {
        color: #333;
      }
      table {
        border-collapse: collapse;
        width: 60%;
        background: #fff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      th,
      td {
        border: 1px solid #ccc;
        padding: 12px 16px;
        text-align: center;
      }
      th {
        background-color: #007bff;
        color: white;
      }
      tr:nth-child(even) {
        background-color: #f9f9f9;
      }
    </style>
  </head>
  <body>
    <h1>🎯 업다운 랭킹</h1>
    <table>
      <thead>
        <tr>
          <th>순위</th>
          <th>유저</th>
          <th>시도 횟수</th>
          <th>걸린 시간</th>
        </tr>
      </thead>
      <tbody>
        <% if (rankings.length === 0) { %>
        <tr>
          <td colspan="4">아직 클리어한 유저가 없습니다 😢</td>
        </tr>
        <% } else { %> <% rankings.forEach(r => { %>
        <tr>
          <td><%= r.rank %></td>
          <td><%= r.user %></td>
          <td><%= r.attempts %></td>
          <td><%= r.time %></td>
        </tr>
        <% }); %> <% } %>
      </tbody>
    </table>
  </body>
</html>
